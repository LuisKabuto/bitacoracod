# Bitácora PRO

Aplicación de gestión operativa para jornada, tareas, supervisión y reportes.

La versión activa de desarrollo se mantiene en la rama `v3-redesign` mientras se migra la interfaz sin modificar la estructura de datos de Firebase/Firestore.